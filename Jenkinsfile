pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'echo "I\'m trying to build"'
        sh 'echo "build step is complete"'
      }
    }

    stage('Testing') {
      steps {
        sh 'echo "Testing has begun"'
      }
    }

  }
}